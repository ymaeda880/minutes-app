# -*- coding: utf-8 -*-
# minutes_app/lib/speaker_prep/explanation.py
# ============================================================
# 話者分離・整形ページ 説明UI
# ============================================================

from __future__ import annotations

# ============================================================# -*- coding: utf-8 -*-
# minutes_app/lib/speaker_prep/explanation.py
# ============================================================
# 話者分離・整形ページ 説明UI
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================
from typing import Any

import streamlit as st

from common_lib.ui.help_expander import render_themed_help_expander
from common_lib.ui.intro_panel import (
    render_info_card_compact,
    render_info_card_bullets_compact_custom,
)


# ============================================================
# ページ上部説明UI
# ============================================================
def render_speaker_prep_page_intro() -> None:

    # ------------------------------------------------------------
    # AI利用・機密情報注意
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
⛔ このページでは，<b>AIを使用</b>しますので，<b>個人情報や機密情報を含む文字起こしテキストの扱いに注意してください</b>．
""",
    )

    # ------------------------------------------------------------
    # ページ概要
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
このページでは，文字起こし済みテキストを読み込み，
話者ごとに発言を分離・整形して，議事録作成に使いやすい逐語録を作成します．
""",
    )

    # ------------------------------------------------------------
    # 使い方
    # ------------------------------------------------------------
    render_info_card_bullets_compact_custom(
        title="使い方",
        items=[
            ("①", "<b>入力方式を選択します．</b>"),
            ("", "通常は，「文字起こし」によりサーバー内部に保存されたテキストファイルを使用します．"),
            ("", "その場合は，「入力方式」で「既存ジョブ」を選択してください．"),
            ("②", "<b>ジョブを選択します．</b>"),
            ("", "「ジョブ選択」から，文字起こしテキストの集合体を選択します．"),
            ("③", "<b>サイドバーでモデル設定とプロンプト設定を確認します．</b>"),
            ("", "通常は初期設定のままで使用できます．"),
            ("④", "<b>「文字起こしテキストを順番に話者分離」ボタンを押します．</b>"),
            ("", "各テキストを順番に話者分離し，個別結果と連結結果を保存します．"),
            ("⑤", "<b>必要に応じて，連結結果をパソコンにダウンロードします．</b>"),
            ("", "後続処理は，サーバー内部ストレージに保存された連結結果から行うこともできます．"),
        ],
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )

    # ------------------------------------------------------------
    # 注意
    # ------------------------------------------------------------
    render_info_card_compact(
        title="注意",
        body_html="""
音声ファイル分割から議事録作成までの一連の作業単位を，このアプリでは「ジョブ」と呼びます．<br>
ここでの「ジョブ」は，主に文字起こしテキストの集合体を指します．
""",
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )


# ============================================================
# public API：詳細説明 expander
# ============================================================
def render_speaker_prep_help_expander(
    *,
    theme: dict[str, Any] | None = None,
    banner_key: str = "light_green",
) -> None:
    render_themed_help_expander(
        expander_key=HELP_EXPANDER_KEY,
        expander_title=HELP_EXPANDER_TITLE,
        tabs=HELP_TABS,
        theme=theme,
        banner_key=banner_key,
        expanded=False,
    )


# ============================================================
# expander 設定
# ============================================================
HELP_EXPANDER_KEY = "speaker_prep_help_expander"
HELP_EXPANDER_TITLE = "📌 説明（クリックで展開）"


# ============================================================
# 詳細説明：話者分離・整形
# ============================================================
SPEAKER_PREP_USAGE_TEXT = """
<div style="line-height:1.7">

## 1. 使い方

1. 文字起こし後のテキストファイルを読み込みます。

   通常は，「文字起こし」によりサーバー内部に保存されたファイルを使用します。

   「入力方式」で「既存ジョブ」を選択し，
   「ジョブ選択」から「ジョブ」を選択します。

2. 左パネル（サイドバー）の「モデル設定」と「プロンプト設定」は，
   通常はデフォルトのままで構いません。

3. 「文字起こしテキストを順番に話者分離（ストレージ保存＋連結）」ボタンを押すと，
   文字起こしテキストの集合体に対して，順番に話者分離が行われます。

4. すべてのファイルの話者分離が終わると，
   それぞれのファイルの話者分離後テキストが結合されます。

5. 話者分離がすべて終わると，
   「ストレージに保存しました」というメッセージが表示されます。

6. この時点で，すべてのファイルに話者分離を行った結果が，
   連結された状態でサーバー内部ストレージに保存されています。

7. 「連結結果をダウンロード」ボタンを押すと，
   話者分離したテキストファイルを1つに連結したものを，
   自分のパソコンにダウンロードできます。

---

## 2. このページの役割（話者分離）

このページは，文字起こしテキスト（.txt）を話者ごとに分離するための前処理ページです。

1つの巨大テキストを一回で整形するのではなく，
複数のファイル（複数 .txt）を順番に処理して保存し，
最後につなぎ行を挟んで連結します。

この連結結果を，後続の議事録作成ページへ渡す想定です。

---

## 3. 入力方式（2通り）

このページは，入力を次の2通りから選べます。

### 3-1. 既存ジョブ（storage より読み込み）

すでに Storages に存在する job を選びます。

選択した job の `transcript/*.txt` を読み込み，
順番に話者分離します。

通常はこちらを使います。

---

### 3-2. 新規アップロード（drop → 新規job作成 → transcript/ に保存）

文字起こしテキスト（.txt）を複数アップロードできます。

アップロード順が，そのまま処理順になります。

重要：アップロードしただけでは job は作りません。

「📦 アップロードを transcript/ として保存して新規ジョブを作成」ボタンを押した時点で，
Storages に新規 job を作成し，`transcript/` に保存します。

---

## 4. 連続処理（バッチ）の流れ

1. 入力を確定します。
   - 既存 job を選択
   - または，新規アップロード → ジョブ作成

2. `transcript/*.txt` を順番に処理します。

3. 話者分離結果をファイルごとに保存します。

<pre>
transcript_speaker_separated/
</pre>

4. 個別結果を「つなぎ行」を挟んで連結し，まとめて保存します。

<pre>
transcript_speaker_separated_combined/
</pre>

つなぎ行は，どのファイルとどのファイルの間かが分かるように挿入されます。

---

## 5. drop を連続して使える運用

新規アップロード（drop）は，何回でも繰り返し行えます。

たとえば，次のように別ジョブとして連続運用できます。

- 午前の会議 → drop → job作成 → 話者分離
- 午後の会議 → drop → job作成 → 話者分離

同じアップロード内容で画面が再実行（rerun）されても，
作成済み job があれば復元して続行できます。

---

## 6. 実行ロジック（バッチ実行）

「文字起こしテキストを順番に話者分離（ストレージ保存＋連結）」を押すと，
次の処理が行われます。

1. 確定済みの job_dir と transcript_files を取得します。
2. transcript を先頭から順に AI に渡します。
3. 結果をファイルごとに保存します。
4. 途中経過を表示しながら最後まで処理します。
5. 最後に連結結果を保存します。
6. 必要に応じて，連結結果をダウンロードできます。

基本は 1ファイルずつ順番に処理します。

ページ側ではリトライは行いません。

---

## 7. 保存先（ストレージ）

選択された job フォルダ配下に保存されます。

- 入力：`transcript/`
- 個別結果：`transcript_speaker_separated/`
- 連結結果：`transcript_speaker_separated_combined/`
- ログ：`logs/process.log`

---

## 8. AI利用について

このページでは AI を使用します。

AI実行は common_lib.ai.routing.call_text を通じて行われます。

ページ側で OpenAI や Gemini の API を直接呼び出しません。

tokens/cost は，AI結果として返ってきた範囲だけを表示・記録します。

ページ側でトークン数や料金を推計しません。

</div>
"""


# ============================================================
# 詳細説明：プロンプト
# ============================================================
PROMPT_TEXT = """
<div style="line-height:1.7">

## 1. プロンプト構成（lib.prompts）

このページでは，`SPEAKER_PREP` を `get_group()` で取得し，
プロンプト UI を組み立てます。

実行時は `build_prompt()` により，次のパートを連結して 1本のプロンプトにします。

1. 必須プロンプト（mandatory）
2. プリセット本文（preset）
3. 追加指示（extra）
4. 入力テキスト

---

## 2. プロンプトの役割

このプロンプトは，文字起こしテキストを話者ごとに分離・整形するために使います。

目的は，後続の議事録作成ページへ渡しやすい逐語録を作ることです。

---

## 3. 話者分離プロンプトの種類

サイドバーでは，話者分離プロンプトを次の3種類から選択できます。

- 標準（精度優先）
- 軽量（タイムアウト低減）
- 超軽量（最小負荷）

---

## 4. 標準（精度優先）

最も詳しい指示を含むプロンプトです。

話者分離の精度を重視する場合に使います。

通常はこの設定を使用します。

---

## 5. 軽量（タイムアウト低減）

標準よりも指示量を減らしたプロンプトです。

入力が長い場合や，処理時間を短くしたい場合に使います。

---

## 6. 超軽量（最小負荷）

最低限の指示だけで処理するプロンプトです。

処理負荷をできるだけ下げたい場合に使います。

ただし，出力の安定性や整形品質は標準より下がる可能性があります。

---

## 7. 追記プリセット

追記プリセットは，話者分離・整形時に追加する補助指示です。

たとえば，次のような内容を指定できます。

- 話者名の表記ルール
- 発言の整理方法
- 不要な記号や重複の扱い
- 議事録作成に渡すための出力形式

---

## 8. 追加指示

追加指示には，今回だけ反映したい任意の指示を書けます。

必要がなければ空欄のままで構いません。

</div>
"""


# ============================================================
# 詳細説明：保存構造
# ============================================================
STORAGE_STRUCTURE_TEXT = """
<div style="line-height:1.7">

## 1. 保存先の基本構造

話者分離・整形結果は，対象ジョブのフォルダ内に保存されます。

基本構造は次の通りです。

<pre>
Storages/&lt;user&gt;/minutes_app/&lt;YYYY-MM-DD&gt;/&lt;job_id&gt;/
</pre>

主なフォルダは次の通りです。

<pre>
transcript/
transcript_speaker_separated/
transcript_speaker_separated_combined/
logs/
job.json
</pre>

---

## 2. transcript フォルダ

`transcript` フォルダには，入力となる文字起こしテキストが保存されています。

前ページの文字起こしで作成された個別結果や，
このページで新規アップロードしたテキストファイルがここに置かれます。

---

## 3. transcript_speaker_separated フォルダ

`transcript_speaker_separated` フォルダには，
AIで話者分離・整形した個別結果が保存されます。

ファイル名には処理順と元のテキストファイル名が含まれます。

---

## 4. transcript_speaker_separated_combined フォルダ

`transcript_speaker_separated_combined` フォルダには，
全テキストファイルを順番につないだ連結結果が保存されます。

後続の議事録作成ページでは，
この連結結果を入力として使う想定です。

---

## 5. logs フォルダ

`logs` フォルダには，`process.log` が保存されます。

処理開始，対象ファイル，保存先，プロンプトレベル，処理完了などの記録が残ります。

---

## 6. job.json

`job.json` には，ジョブID，ユーザー，保存パス，処理ステータスなどが保存されます。

後続ページが同じジョブを参照するための基礎情報になります。

</div>
"""


# ============================================================
# 詳細説明：モデル・料金
# ============================================================
MODEL_COST_TEXT = """
<div style="line-height:1.7">

## 1. モデル設定

このページでは，サイドバーでテキスト生成モデルを選択します。

利用できるモデル一覧は，common_lib.ai.models の TEXT_MODEL_CATALOG を正本とします。

ページ側でモデル一覧を直接定義しません。

---

## 2. provider:model 形式

モデル選択UIの戻り値は，provider:model 形式です。

例：

- openai:gpt-5-mini
- openai:gpt-5
- gemini:gemini-2.5-flash

この文字列から provider と model を分解し，
common_lib.ai.routing.call_text に渡します。

---

## 3. 料金・トークン表示

このページでは，ページ側で料金やトークン数を推計しません。

AI結果として usage や cost が返ってきた場合のみ，
その値を合算して表示・記録します。

---

## 4. 実行記録

話者分離・整形の実行は，busy_run により ai_runs.db に記録されます。

複数ファイルを処理する場合でも，
1回のバッチ処理を 1 run として扱います。

実行後は run_summary により，
AI使用時間，入力トークン数，出力トークン数，料金などを確認できます。

</div>
"""


# ============================================================
# 詳細説明：FAQ
# ============================================================
FAQ_TEXT = """
<div style="line-height:1.7">

## 1. よくある質問（FAQ）

### Q1. 通常はどの入力方式を使えばよいですか？

通常は「既存ジョブ」を使います。

前ページの文字起こしで作成された transcript/ のテキストを，
そのまま読み込むことができます。

---

### Q2. サイドバーの設定は変更する必要がありますか？

通常は変更する必要はありません。

まずは，モデルとプロンプトレベルを初期設定のまま実行してください。

---

### Q3. プロンプトレベルはどれを使えばよいですか？

通常は「標準（精度優先）」を使います。

入力が長くて処理が失敗しやすい場合は，
「軽量（タイムアウト低減）」または「超軽量（最小負荷）」を試してください。

---

### Q4. 話者分離結果はどこに保存されますか？

個別結果は `transcript_speaker_separated/` に保存されます。

全体を結合したテキストは `transcript_speaker_separated_combined/` に保存されます。

---

### Q5. 連結結果をダウンロードしないと次の処理に進めませんか？

いいえ。

後続処理は，サーバー内部ストレージに保存された連結結果から行えます。

パソコンに保存したい場合だけ，連結結果をダウンロードしてください。

---

### Q6. 新規アップロードは何回でも使えますか？

はい。

午前の会議，午後の会議のように，
別々のアップロードを別ジョブとして作成できます。

---

### Q7. AI料金は必ず表示されますか？

必ず表示されるとは限りません。

AI結果として cost が返ってきた場合だけ，料金を表示します。

取得できない場合は，推計せずに空欄または注記として扱います。

</div>
"""


# ============================================================
# 詳細説明タブ
# ============================================================
HELP_TABS = [
    ("話者分離・整形（使い方）", SPEAKER_PREP_USAGE_TEXT),
    ("プロンプト", PROMPT_TEXT),
    ("保存構造", STORAGE_STRUCTURE_TEXT),
    ("モデル/料金", MODEL_COST_TEXT),
    ("FAQ", FAQ_TEXT),
]
# imports
# ============================================================
from typing import Any

import streamlit as st

from common_lib.ui.help_expander import render_themed_help_expander
from common_lib.ui.intro_panel import (
    render_info_card_compact,
    render_info_card_bullets_compact_custom,
)


# ============================================================
# ページ上部説明UI
# ============================================================
def render_speaker_prep_page_intro() -> None:

    # ------------------------------------------------------------
    # AI利用・機密情報注意
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
⛔ このページでは，<b>AIを使用</b>しますので，<b>個人情報や機密情報を含む文字起こしテキストの扱いに注意してください</b>．
""",
    )

    # ------------------------------------------------------------
    # ページ概要
    # ------------------------------------------------------------
    render_info_card_compact(
body_html="""
このページでは，文字起こし済みテキストを読み込み，話者ごとに発言を分離・整形して，人手による逐語録作成の叩き台を作成します．
""",
    )

    # ------------------------------------------------------------
    # 使い方
    # ------------------------------------------------------------
    render_info_card_bullets_compact_custom(
        title="使い方",
        items=[
("①", "<b>「テキストファイルの設定」で話者分離を行うテキストファイルを設定します．</b>"),
("", "通常は，前ページの文字起こしで作成された「既存ジョブ」を選択します．"),
("", "新しく文字起こしテキストを直接アップロードして処理することもできます．"),
("②", "<b>「話者分離の実行」では，AIによる話者分離を実行します．</b>"
+"各テキストを順番に話者分離し，個別結果と連結結果を<b>自動的にサーバー内部に保存します</b>．"),
("","必要に応じて，「プロンプト（クリックで開く）」よりプロンプトの追加が行えます．"),
("", "必要に応じて，連結結果をパソコンにダウンロードできます．"),
("", "⚠️ サイドバーでモデルとプロンプトの設定ができますが，通常は初期設定のままで使用できます．"),
        ],
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )

    # ------------------------------------------------------------
    # 注意
    # ------------------------------------------------------------
    render_info_card_compact(
        title="注意",
        body_html="""
音声ファイル分割から議事録作成までの一連の作業単位を，このアプリでは「ジョブ」と呼びます．<br>
ここでの「ジョブ」は，主に文字起こしテキストの集合体を指します．
""",
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )


# ============================================================
# public API：詳細説明 expander
# ============================================================
def render_speaker_prep_help_expander(
    *,
    theme: dict[str, Any] | None = None,
    banner_key: str = "light_green",
) -> None:
    render_themed_help_expander(
        expander_key=HELP_EXPANDER_KEY,
        expander_title=HELP_EXPANDER_TITLE,
        tabs=HELP_TABS,
        theme=theme,
        banner_key=banner_key,
        expanded=False,
    )


# ============================================================
# expander 設定
# ============================================================
HELP_EXPANDER_KEY = "speaker_prep_help_expander"
HELP_EXPANDER_TITLE = "詳細説明（クリックで展開）"


# ============================================================
# 詳細説明：話者分離・整形
# ============================================================
SPEAKER_PREP_USAGE_TEXT = """
<div style="line-height:1.7">

## 1. このページの役割

このページは，文字起こしされたテキストを，
議事録作成に使いやすい形へ整えるためのページです。

主に，前ページの「文字起こし」で作成された transcript/ のテキストを読み込み，
AIを使って話者ごとに発言を分離・整形します。

処理結果は，個別ファイルとして保存されるとともに，
全体を1つにつないだ連結テキストとしても保存されます。

---

## 2. 全体の処理の流れ

1. 処理を行うテキストファイルを選択します。
2. 既存ジョブ，または新規アップロードテキストを選択します。
3. 対象となる transcript/*.txt を確認します。
4. サイドバーでモデルを確認します。
5. サイドバーで話者分離プロンプトの種類を選択します。
6. 必要に応じて，プロンプト本文や追加指示を編集します。
7. 「文字起こしテキストを順番に話者分離」ボタンを押します。
8. 各テキストファイルを順番にAIで処理します。
9. 個別結果を transcript_speaker_separated/ に保存します。
10. 全体を結合した結果を transcript_speaker_separated_combined/ に保存します。
11. 画面上で実行サマリ，トークン数，料金，保存先を確認できます。

---

## 3. 入力方式

### 3-1. 既存ジョブ

通常はこちらを使います。

前ページの「文字起こし」で作成されたジョブを選択し，
そのジョブの transcript/ フォルダに保存されているテキストファイルを読み込みます。

この方式では，文字起こしテキストを再アップロードする必要はありません。

---

### 3-2. 新規アップロード

文字起こし済みテキストをこのページで直接アップロードする方式です。

複数の .txt ファイルをアップロードすると，
アップロード順に番号を付けて，新しいジョブの transcript/ フォルダへ保存します。

その後，保存されたテキストファイルを対象として話者分離・整形を実行します。

---

## 4. ジョブとは

このアプリでは，音声ファイル分割から文字起こし，話者分離・整形，議事録作成までの一連の作業単位を
「ジョブ」と呼びます。

このページでのジョブは，主に次の内容を含みます。

- 文字起こしテキスト
- 話者分離済みテキスト
- 話者分離済み連結テキスト
- 処理ログ
- job.json

---

## 5. このページで作成されるテキスト

このページでは，文字起こしテキストをもとに，
次のような形の逐語録を作成します。

- 話者ごとに発言を分ける
- 発言の順番を保つ
- 不自然な改行や重複を整理する
- 議事録作成ページに渡しやすい形に整える

---

## 6. 連結結果

複数のテキストファイルを処理した場合は，
各ファイルの処理結果を1つのテキストに連結します。

連結時には，ファイルの境界が分かるように，
つなぎ目マーカーを挿入します。

<pre>
----- ここがつなぎ目です（ファイル名 と次のファイルの間）-----
</pre>

この連結結果は，後続の議事録作成ページで使う想定です。

---

## 7. AI利用について

このページでは AI を使用します。

AI実行は common_lib.ai.routing.call_text を通じて行われます。

ページ側で OpenAI や Gemini の API を直接呼び出しません。

tokens/cost は，AI結果として返ってきた範囲だけを表示・記録します。

ページ側でトークン数や料金を推計しません。

</div>
"""


# ============================================================
# 詳細説明：プロンプト
# ============================================================
PROMPT_TEXT = """
<div style="line-height:1.7">

## 1. プロンプトの役割

このページでは，文字起こしテキストを話者ごとに分離・整形するために，
AIへプロンプトを渡します。

プロンプトは，主に次の4つから構成されます。

1. 必須プロンプト
2. 追記プリセット
3. 追加指示
4. 入力テキスト

---

## 2. 話者分離プロンプトの種類

サイドバーでは，話者分離プロンプトを次の3種類から選択できます。

- 標準（精度優先）
- 軽量（タイムアウト低減）
- 超軽量（最小負荷）

---

## 3. 標準（精度優先）

最も詳しい指示を含むプロンプトです。

話者分離の精度を重視する場合に使います。

通常はこの設定を使用します。

---

## 4. 軽量（タイムアウト低減）

標準よりも指示量を減らしたプロンプトです。

入力が長い場合や，処理時間を短くしたい場合に使います。

---

## 5. 超軽量（最小負荷）

最低限の指示だけで処理するプロンプトです。

処理負荷をできるだけ下げたい場合に使います。

ただし，出力の安定性や整形品質は標準より下がる可能性があります。

---

## 6. 追記プリセット

追記プリセットは，話者分離・整形時に追加する補助指示です。

たとえば，次のような内容を指定できます。

- 話者名の表記ルール
- 発言の整理方法
- 不要な記号や重複の扱い
- 議事録作成に渡すための出力形式

---

## 7. 追加指示

追加指示には，今回だけ反映したい任意の指示を書けます。

必要がなければ空欄のままで構いません。

---

## 8. 最終プロンプトの組み立て

実行時には，次の内容を結合して1本のプロンプトを作ります。

1. 選択された必須プロンプト
2. 選択された追記プリセット本文
3. 追加指示
4. 入力テキスト本体

この最終プロンプトを AI に渡し，話者分離・整形結果を得ます。

</div>
"""


# ============================================================
# 詳細説明：保存構造
# ============================================================
STORAGE_STRUCTURE_TEXT = """
<div style="line-height:1.7">

## 1. 保存先の基本構造

話者分離・整形結果は，対象ジョブのフォルダ内に保存されます。

基本構造は次の通りです。

<pre>
Storages/&lt;user&gt;/minutes_app/&lt;YYYY-MM-DD&gt;/&lt;job_id&gt;/
</pre>

主なフォルダは次の通りです。

<pre>
transcript/
transcript_speaker_separated/
transcript_speaker_separated_combined/
logs/
job.json
</pre>

---

## 2. transcript フォルダ

`transcript` フォルダには，入力となる文字起こしテキストが保存されています。

前ページの文字起こしで作成された個別結果や，
このページで新規アップロードしたテキストファイルがここに置かれます。

---

## 3. transcript_speaker_separated フォルダ

`transcript_speaker_separated` フォルダには，
AIで話者分離・整形した個別結果が保存されます。

ファイル名には処理順と元のテキストファイル名が含まれます。

---

## 4. transcript_speaker_separated_combined フォルダ

`transcript_speaker_separated_combined` フォルダには，
全テキストファイルを順番につないだ連結結果が保存されます。

後続の議事録作成ページでは，
この連結結果を入力として使う想定です。

---

## 5. logs フォルダ

`logs` フォルダには，`process.log` が保存されます。

処理開始，対象ファイル，保存先，プロンプトレベル，処理完了などの記録が残ります。

---

## 6. job.json

`job.json` には，ジョブID，ユーザー，保存パス，処理ステータスなどが保存されます。

後続ページが同じジョブを参照するための基礎情報になります。

</div>
"""


# ============================================================
# 詳細説明：モデル・料金
# ============================================================
MODEL_COST_TEXT = """
<div style="line-height:1.7">

## 1. モデル設定

このページでは，サイドバーでテキスト生成モデルを選択します。

利用できるモデル一覧は，common_lib.ai.models の TEXT_MODEL_CATALOG を正本とします。

ページ側でモデル一覧を直接定義しません。

---

## 2. provider:model 形式

モデル選択UIの戻り値は，provider:model 形式です。

例：

- openai:gpt-5-mini
- openai:gpt-5
- gemini:gemini-2.5-flash

この文字列から provider と model を分解し，
common_lib.ai.routing.call_text に渡します。

---

## 3. 料金・トークン表示

このページでは，ページ側で料金やトークン数を推計しません。

AI結果として usage や cost が返ってきた場合のみ，
その値を合算して表示・記録します。

---

## 4. 実行記録

話者分離・整形の実行は，busy_run により ai_runs.db に記録されます。

複数ファイルを処理する場合でも，
1回のバッチ処理を 1 run として扱います。

実行後は run_summary により，
AI使用時間，入力トークン数，出力トークン数，料金などを確認できます。

</div>
"""


# ============================================================
# 詳細説明：FAQ
# ============================================================
FAQ_TEXT = """
<div style="line-height:1.7">

## 1. よくある質問（FAQ）

### Q1. 通常はどの入力方式を使えばよいですか？

通常は「既存ジョブ」を使います。

前ページの文字起こしで作成された transcript/ のテキストを，
そのまま読み込むことができます。

---

### Q2. サイドバーの設定は変更する必要がありますか？

通常は変更する必要はありません。

まずは，モデルとプロンプトレベルを初期設定のまま実行してください。

---

### Q3. プロンプトレベルはどれを使えばよいですか？

通常は「標準（精度優先）」を使います。

入力が長くて処理が失敗しやすい場合は，
「軽量（タイムアウト低減）」または「超軽量（最小負荷）」を試してください。

---

### Q4. 話者分離結果はどこに保存されますか？

個別結果は `transcript_speaker_separated/` に保存されます。

全体を結合したテキストは `transcript_speaker_separated_combined/` に保存されます。

---

### Q5. 連結結果をダウンロードしないと次の処理に進めませんか？

いいえ。

後続処理は，サーバー内部ストレージに保存された連結結果から行えます。

パソコンに保存したい場合だけ，連結結果をダウンロードしてください。

---

### Q6. AI料金は必ず表示されますか？

必ず表示されるとは限りません。

AI結果として cost が返ってきた場合だけ，料金を表示します。

取得できない場合は，推計せずに空欄または注記として扱います。

</div>
"""


# ============================================================
# 詳細説明タブ
# ============================================================
HELP_TABS = [
    ("話者分離・整形（使い方）", SPEAKER_PREP_USAGE_TEXT),
    ("プロンプト", PROMPT_TEXT),
    ("保存構造", STORAGE_STRUCTURE_TEXT),
    ("モデル/料金", MODEL_COST_TEXT),
    ("FAQ", FAQ_TEXT),
]