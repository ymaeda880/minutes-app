# -*- coding: utf-8 -*-
# minutes_app/lib/summarized_transcript/explanation.py
# ============================================================
# 要約逐語録作成ページ 説明UI
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================
from typing import Any

import streamlit as st

from common_lib.ui.help_expander import render_themed_help_expander
from common_lib.ui.intro_panel import (
    render_info_card_compact,
    render_info_card_bullets_compact_custom,
)


# ============================================================
# ページ上部説明UI
# ============================================================
def render_summarized_transcript_page_intro() -> None:

    # ------------------------------------------------------------
    # AI利用・機密情報注意
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
⛔ このページでは，<b>AIを使用</b>しますので，<b>個人情報や機密情報の入力は避けてください</b>．
""",
    )

    # ------------------------------------------------------------
    # ページ概要
    # ------------------------------------------------------------
    render_info_card_compact(
        body_html="""
このページでは，人手で作成された逐語録を入力として，要約逐語録をAIで生成します．
""",
    )

    # ------------------------------------------------------------
    # 使い方
    # ------------------------------------------------------------
    render_info_card_bullets_compact_custom(
        title="使い方",
        items=[
("①", "<b>「逐語録入力」</b>から逐語録をアップロードするか，直接貼り付けます．"
+"アップロードはパソコンまたはInboxから行うことができます．"),
("", "アップロード後，「セット」を行ってください．"),
("", "⚠️ サイドバーで<b>発言分割の設定を行えます．</b>"+
"発言者と発言内容の区切り文字を設定します．デフォルトは全角コロン（：）と半角コロン（:）です．"+
"また，1ファイルの最大発言数を設定できます．デフォルトは80発言です．"),
("②", "<b>「要約逐語録の設定」</b>で，見た目のスタイル，追記プリセット，追加指示を設定します．"),
("③", "「発言解析」でファイルの分割状況が確認できます．"),
("④", "<b>「要約逐語録を作成」ボタンを押して要約逐語録を作成します．</b>"),
("⑤", "作成後，要約逐語録，発言番号付き逐語録，要約処理説明などをテキスト形式で保存できます．"),
        ],
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )

    # ------------------------------------------------------------
    # 継続改善
    # ------------------------------------------------------------
    render_info_card_compact(
        title="お願い",
        body_html="""
    満足のいかない結果が出た時は，再度，要約逐語録を作成して比較してください．
    モデルを変更しながら，試してみることも効果的です．
    また，要約できていない部分のみを切り出して，その部分のみの要約逐語録を作成し，最後に人手でつなげることも効果的です．
    AIの生成結果には揺れがありますので，数回試して比較してください．
    """,
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )


# ============================================================
# public API：詳細説明 expander
# ============================================================
def render_summarized_transcript_help_expander(
    *,
    theme: dict[str, Any] | None = None,
    banner_key: str = "light_green",
) -> None:
    render_themed_help_expander(
        expander_key=HELP_EXPANDER_KEY,
        expander_title=HELP_EXPANDER_TITLE,
        tabs=HELP_TABS,
        theme=theme,
        banner_key=banner_key,
        expanded=False,
    )


# ============================================================
# expander 設定
# ============================================================
HELP_EXPANDER_KEY = "summarized_transcript_help_expander"
HELP_EXPANDER_TITLE = "詳細説明（クリックで展開）"


# ============================================================
# 詳細説明：使い方
# ============================================================
SUMMARY_TRANSCRIPT_USAGE_TEXT = """

##### 1. このページの役割

- 人手で作成された **逐語録** を入力として，**要約逐語録** を生成するページです。

- 通常の議事録ではなく，発言者ごとの会話の流れを残しながら，長い逐語録を読みやすく整理することを目的としています。

---

##### 2. 全体の処理の流れ

1. 左カラム（サイドバー）で **AIモデル** を選択します。  
   原則として，デフォルトのままで構いません。

2. **逐語録入力** から逐語録をアップロードするか，直接貼り付けます。  
   アップロードはパソコンまたはInboxから行うことができます。

3. 発言者と発言内容を区切る文字を設定します。  
   デフォルトは，全角コロン（`：`）および半角コロン（`:`）です。

4. 1ファイルの最大発言数を設定します。  
   デフォルトは80発言です。

5. システムが逐語録を解析し，発言者行を検出して発言単位へ変換します。  
   この処理にはAIを使用しません。

6. 発言ごとに連番（0001，0002，...）を付与します。

7. 指定した発言数ごとに，番号付き逐語録を分割します。  
   この分割処理にもAIを使用しません。

8. 各チャンクをAIに渡し，要約逐語録を作成します。

9. AIは同時に，省略した発言番号と統合した発言番号を記録します。

10. 各チャンクの要約逐語録を，時系列順に結合します。  
    最後に全体をAIで再編集することはしません。

11. 結果をダウンロードします。

---

##### 3. 出力されるファイル

このページでは，主に次のファイルを出力します。

- 要約逐語録.txt
- 発言番号付き逐語録.txt
- 要約処理説明.txt
- 分割情報.txt

</div>
"""


# ============================================================
# 詳細説明：発言抽出と分割
# ============================================================
SUMMARY_TRANSCRIPT_SPLIT_TEXT = """

##### 1. 発言抽出について

発言抽出にはAIを使用しません。

逐語録から，次の形式の発言者行を検出します。

`発言者：発言内容`

または

`発言者:発言内容`

発言者行を検出した後，次の発言者行が現れるまでを1発言として扱います。

そのため，本文が複数行にまたがる場合でも，同じ発言として結合されます。

---

##### 2. 区切り文字について

デフォルトでは，次の2種類を発言者と発言内容の区切り文字として認識します。

- 全角コロン（`：`）
- 半角コロン（`:`）

それ以外の記号を使う逐語録の場合は，サイドバーでカスタム区切り文字を指定できます。

---

##### 3. 発言番号について

抽出された発言には，次のような連番を付与します。

`【0001】`

`【0002】`

`【0003】`

この番号は，会議の時系列を表します。

AIには，この番号付き逐語録を入力します。

---

##### 4. 発言数による分割について

長い逐語録を一度にAIへ入力すると，時系列の保持が難しくなる場合があります。

そのため，このページでは，発言数を指定して逐語録を分割します。

例：

- チャンク1：0001～0080
- チャンク2：0081～0160
- チャンク3：0161～0240

この分割処理にもAIは使用しません。

</div>
"""


# ============================================================
# 詳細説明：AI処理
# ============================================================
SUMMARY_TRANSCRIPT_AI_TEXT = """

##### 1. AIに渡す入力

AIには，番号付き逐語録を渡します。

例：

`【0001】`  
`渡辺会長：では始めましょう。`

`【0002】`  
`山田委員：今回の議題は何ですか。`

`【0003】`  
`内田議長：今回の議題はマーケティングです。`

AIは，この発言番号を参照しながら，要約逐語録を作成します。

---

##### 2. 時系列保持

発言番号の順番は，会議の時系列そのものです。

そのため，AIには次のルールを与えます。

- 後の発言を前に移動しない
- 前の発言を後に移動しない
- 別の論点を挟んだ発言を無理に統合しない
- 逐語録にない情報を追加しない

---

##### 3. 要約逐語録の本文

要約逐語録では，発言者名を残しながら，発言内容を読みやすく整理します。

出力形式は，原則として次の形です。

`発言者名：発言の要旨`

質問は質問として残し，回答は回答として残します。

単なる要点メモではなく，会話の流れが分かる文章として整理します。

---

##### 4. 省略と統合

AIは，次のような発言を省略できます。

- 単なる相槌
- 内容のない確認
- 同趣旨の繰り返し
- 直前または直後の発言に吸収できる発言

また，次のような発言は統合できます。

- 同一話者による連続的な説明
- 同一論点に関する短い補足
- 相槌や確認を挟んだ実質的に連続する発言

ただし，省略や統合を行った場合は，必ず要約処理説明に記録します。

---

##### 5. 要約処理説明

要約処理説明には，次の内容が記録されます。

- 省略した発言番号
- 省略理由
- 統合した発言番号
- 統合理由

例：

省略

- 0005  
  理由：相槌のみのため省略

統合

- 0004 + 0006  
  理由：同一話者による同一論点の発言として統合

</div>
"""


# ============================================================
# 詳細説明タブ
# ============================================================
HELP_TABS = [
    ("要約逐語録作成（使い方）", SUMMARY_TRANSCRIPT_USAGE_TEXT),
    ("発言抽出と分割", SUMMARY_TRANSCRIPT_SPLIT_TEXT),
    ("AI処理と処理説明", SUMMARY_TRANSCRIPT_AI_TEXT),
]