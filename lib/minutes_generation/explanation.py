# -*- coding: utf-8 -*-
# minutes_app/lib/minutes_generation/explanation.py
# ============================================================
# 議事録作成ページ 説明UI
# ============================================================

from __future__ import annotations

# ============================================================
# imports
# ============================================================
from typing import Any

import streamlit as st

from common_lib.ui.help_expander import render_themed_help_expander
from common_lib.ui.intro_panel import (
    render_info_card_compact,
    render_info_card_bullets_compact_custom,
)


# ============================================================
# ページ上部説明UI
# ============================================================
def render_minutes_page_intro() -> None:

    # ------------------------------------------------------------
    # AI利用・機密情報注意
    # ------------------------------------------------------------
    render_info_card_compact(
body_html="""
⛔ このページでは，<b>AIを使用</b>しますので，<b>個人情報や機密情報の入力は避けてください</b>．
""",
    )

    # ------------------------------------------------------------
    # ページ概要
    # ------------------------------------------------------------
    render_info_card_compact(
body_html="""
このページでは，人手で作成された逐語録を入力として，議事録をAIで生成します．
""",
    )

    # ------------------------------------------------------------
    # 使い方
    # ------------------------------------------------------------
    render_info_card_bullets_compact_custom(
        title="使い方",
        items=[
            ("①", "<b>「逐語録入力」</b>から逐語録をアップロードするか，直接貼り付けます．</b>"),
            ("", "アップロードはパソコンまたはInboxから行うことができます．"),
            ("", "アップロード後，「セット」を行ってください．"),
            ("②", "<b>議事録形式の設定を行います．</b>"),
            ("", "「議事録の種類」や「見た目のスタイル」を設定します．"),
            ("", "<b>「追加指示」では任意のプロンプトを追加することができます．</b>必要のない時は空欄のままで構いません．"),
            ("③", "<b>「議事録を生成」ボタンを押してください．</b>生成AIを用いて議事録を生成します．"),
            ("④", "「議事録の保存」では，テキストとWord形式で議事録がパソコンに保存できます．"),
        ],
    )

    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )

    # ------------------------------------------------------------
    # 継続改善
    # ------------------------------------------------------------
    render_info_card_compact(
        title="お願い",
        body_html="""
　AIの出力には毎回ある程度の揺れがあります．満足できない結果が得られた場合は，再度実行して，比較してみてください．
""",
    )
    st.markdown(
        "<div style='height:16px'></div>",
        unsafe_allow_html=True,
    )


# ============================================================
# public API：詳細説明 expander
# ============================================================
def render_minutes_help_expander(
    *,
    theme: dict[str, Any] | None = None,
    banner_key: str = "light_green",
) -> None:
    render_themed_help_expander(
        expander_key=HELP_EXPANDER_KEY,
        expander_title=HELP_EXPANDER_TITLE,
        tabs=HELP_TABS,
        theme=theme,
        banner_key=banner_key,
        expanded=False,
    )


# ============================================================
# expander 設定
# ============================================================
HELP_EXPANDER_KEY = "minutes_generation_help_expander"
HELP_EXPANDER_TITLE = "詳細説明（クリックで展開）"


# ============================================================
# 詳細説明：議事録作成
# ============================================================
MINUTES_USAGE_TEXT = """
<div style="line-height:1.7">

## 1. このページの役割

- 人手で作成された
  **逐語録** を入力として、
  **正式な議事録を生成するページ**です。

---

## 2. 全体の処理の流れ

1. 左カラム（サイドバー）で **AIモデル** とを選びます。
（原則，デフォルトのままで良いです．特に変更する必要はありません．）

2. 右カラム（メインエリア）の **逐語録入力** から逐語録をアップロードするか、直接貼り付けます。
（アップロードはパソコンまたはInboxから行うことができます．）
アップロード後，「読み込み」および「セット」を行ってください．

3. **議事録形式の設定** では，まず議事録の種類をボタン（ラジオボタン）で選択します．その後「見た目のスタイル」「追記プリセット」を設定します．

4. **追加指示** では任意のプロンプトを追加することができます．必要のない時は空欄のままで構いません．

5. 「議事録を生成」ボタンを押して、生成AIを用いて議事録を生成します。

6. 生成結果が画面に表示され，料金・トークン数，AI使用時間などを確認できます。

7. **議事録の保存** では，テキストとWord形式で議事録がパソコンに保存できます．


</div>
"""


# ============================================================
# 詳細説明：プロンプト構造
# ============================================================
PROMPT_STRUCTURE_TEXT = """
<div style="line-height:1.7">

## 1. プロンプト構造（内容 × 見た目の二段構成）

このページのプロンプトは、次のような構造になっています。

### 1-1. 内容系（MINUTES_MAKER グループ）

- `MINUTES_MANDATORY_MODES` で  
  「逐語録 / 逐語録のまとめ / 簡易議事録 / 詳細議事録」などの  
  **議事録の種類ごとの必須パート**を定義しています。

- ラジオボタン「議事録の種類」でモードを選ぶと、
  対応する必須パートが `minutes_mandatory` に自動反映されます。

- さらに `lib.prompts` の `MINUTES_MAKER` グループから
  - 追記プリセット（内容）
  - その本文（編集可）
  を取得して、**内容面の補足指示**を組み立てます。

### 1-2. 見た目スタイル（MINUTES_STYLE グループ）

- `MINUTES_STYLE` グループには、
  - ベーシックな箇条書き
  - 横線やセクション罫線を多用するスタイル
  - Word 見出しとの相性を重視したスタイル
  など、**見た目（レイアウト）に関する指示**をまとめています。

- ラジオボタン「見た目のスタイル」でラベルを選ぶと、
  その本文を `style_body = style_group.body_for_label(...)` で取得します。

### 1-3. 内容プリセット + 見た目スタイルの合体

- まず「内容プリセット」（`minutes_preset_text`）をベースにし、

- そこに

【見た目のスタイル指示】  
（MINUTES_STYLE 側の本文）

を後ろに付けたものを `merged_preset` として使います。

### 1-4. 最終プロンプトの組み立て

- `build_prompt()` に渡す 4つの要素は：

1. `minutes_mandatory`（議事録の種類に応じた必須パート）
2. `merged_preset`（内容プリセット + 見た目スタイル指示）
3. `minutes_extra_text`（任意の追加指示）
4. `src`（整形済みテキスト本体）

- これらを 1本の長いプロンプトに連結して、LLM に渡します。

</div>
"""


# ============================================================
# 詳細説明：モデル / 料金
# ============================================================
MODEL_COST_TEXT = """
<div style="line-height:1.7">

## 1. モデル設定と温度・トークン上限

サイドバーで次を指定します。

- モデル：
  - `gpt-5`
  - `gpt-5-mini`
  - `gpt-5-nano`
  - `gpt-4.1-mini`
  - `gpt-4.1`

- 温度（0〜2）：
  - GPT-5 系は `supports_temperature()` により **temperature 固定** とみなし、
    スライダーを無効化 & API に temperature を送信しません。

  - GPT-4.1 系は、1.0と異なる場合のみ
    `chat_kwargs["temperature"]` に設定します。

- `max_completion_tokens`：
  - modern の `max_completion_tokens` としてそのまま渡されます。
  - 長めの議事録を一度に生成したい場合は 8,000〜12,000 程度を推奨。

---

## 2. モデル呼び出し（リトライなし一発実行）

「📝 議事録を生成」ボタンを押すと：

1. 上述のプロンプト（combined）を作成。

2. `call_once()` を通じて
   `client.chat.completions.create()` を **1回だけ**実行。

- 引数：
  - `model`
  - `messages=[{"role": "user", "content": combined}]`
  - `max_completion_tokens`
  - （必要に応じて）`temperature`

3. `resp.choices[0].message.content`
（または `.text`）から議事録本文を取得。

4. 結果が空でなければ
   `minutes_final_output`
   として `session_state` に保存し、
   Markdown で表示します。

5. `finish_reason == "length"` の場合は、
   「出力上限で切れている可能性あり」
   として info を表示します。

6. 結果が空の場合は、
   レスポンス全体 (`resp.model_dump()`)
   を表示してデバッグできます。

---

## 3. トークン・料金の概要

- `extract_tokens_from_response(resp)` で
  **入力トークン / 出力トークン / 合計トークン**
  を取得します。

- `estimate_chat_cost_usd(model, input_tok, output_tok)`
  で USD の概算コストを計算し、
  サイドバーの `USD/JPY` レートを掛けて
  **JPY の概算** を出します。

- これらを小さな表として表示し、
  実行ごとのコストを把握しやすくしています。

- さらに「🔍 トークン算出の内訳」expander では、
  `debug_usage_snapshot(resp.usage)`
  により modern usage の中身を確認できます。

---

## 4. 生成結果の保存（.txt / .docx）

### 4-1. テキスト保存（.txt）

- `minutes_final_output`
  を UTF-8 でエンコードし、
  「💾 テキストで保存 (.txt)」
  ボタンからダウンロードできます。

### 4-2. Word 保存（.docx）

- `python-docx` が利用可能な場合：
  - Markdown 的な構造を簡易に解釈して Word スタイルに変換します。

- 主なルール：
  - 空行 → 空段落
  - `---` / `―――` / `ーーー` → 段落下罫線（pBdr）
  - `# 見出し` → Heading 1
  - `## 見出し` → Heading 2
  - `- 箇条書き` → List Bullet スタイル
  - その他の行 → Normal スタイル

- フォントはデフォルトで「游ゴシック」を使用します。

</div>
"""

# ============================================================
# 詳細説明：GPTに送るプロンプト
# ============================================================
PROMPT_TEXT = """
<div style="line-height:1.7">

## Minutes Maker に渡されるプロンプト構造（GPT への最終プロンプト）

このページでは，Minutes Maker から GPT に送られるプロンプト原文を，構成要素ごとにそのままの形で示します。  
実際の呼び出しでは，下記 1〜4 をこの順番で連結したものが 1 本のプロンプトとして送信されます。

1. Minutes Maker 全体共通 mandatory（`MINUTES_GLOBAL_MANDATORY`）
2. モード別 mandatory（逐語録／簡易議事録／詳細議事録のいずれか 1 本）
3. 追記プリセット＋見た目スタイル（`merged_preset`）
4. 追加指示（任意）と入力テキスト（整形済みテキスト）

---

## 1. Minutes Maker 全体共通 mandatory

`MINUTES_GLOBAL_MANDATORY` の原文は次の通りです。

---

### 1-1. MINUTES_GLOBAL_MANDATORY 本文

あなたは日本語の会議議事録（逐語録・簡易議事録・詳細議事録）を作成する専門家です。  
以下の入力テキスト（整形済みテキスト：話者ごとに分離されたテキスト）をもとに，  
指定されたモードに応じた形式の議事録を出力してください。

【共通方針】

- 事実関係を改変してはいけません。推測や補完を行う場合は，「〜と考えられる」「〜と思われる」など不確実性が分かる表現で示してください。
- 日付・時刻・数値は，原則として半角で統一してください（例: 2025-12-08, 10:30, 3 件）。
- 固有名詞（人名・地名・組織名・資料名など）は，入力テキスト中の表記をできるだけ尊重してください。
- 同じ内容を繰り返し説明しないようにし，読み手が「何が議論され，何が決まり，次に何をするのか」を素早く把握できる構成を心がけてください。

【書式・見た目に関する共通ルール】

- 見出しには Markdown の見出し記法（`#`, `##`, `###`）を使用してください（行頭に全角スペースを入れない）。
- 箇条書きは「`- `」（半角ハイフン＋半角スペース）を用いてください。Tab は使わず，必要に応じて半角スペースでインデントしてください。
- 行頭に「*」「●」「■」などの記号を用いた独自の箇条書きは使わないでください。
- 不要な空行は避け，見出しやセクションの区切りの前後など，意味のある箇所にだけ空行を入れてください。

【表の出力に関する共通ルール（重要）】

- 「変更指示一覧」「TODO 一覧」「決定事項一覧」など，表形式の出力が必要な場合は，必ず Markdown の表記法を用いて出力してください。
- 表の構造は次の形式に従ってください（例）：

  | 列1 | 列2 | 列3 |
  | --- | --- | --- |
  | 値1 | 値2 | 値3 |

  ※この例は構造を示すだけであり，実際の列名・列数は指示に応じて変更して構いません。

- 表に関する厳守事項:
  - 各行の先頭と末尾，および列の区切りには半角の縦棒 `|` を使ってください。全角の「｜」は絶対に使わないでください。
  - 行頭に余分な空白（特に全角スペース）を入れないでください。
  - コードブロック（``` で囲む形式）は使わないでください。表そのものだけを出力してください。
  - セル内で改行しないでください。複数文が必要な場合でも 1 セル内に 1 つの段落としてまとめてください。
  - セル内に `|` を含める必要がある場合は，必ず `\\|` とエスケープしてください。
  - 空欄セルが必要なときは完全な空文字ではなく，半角スペース 1 つを入れておいてください。

- 「表形式だけを出力してください」「表のみを返してください」などと明示された場合は，上記の Markdown 表以外のテキスト（説明文・見出し・前置きなど）を書いてはいけません。  
  そうでない場合は，議事録本文のなかに必要に応じて表を挿入して構いません。

これらの共通ルールは，逐語録・簡易議事録・詳細議事録のいずれのモードでも必ず守ってください。

---

（以下、入力した逐語録が続く）

</div>
"""

# ============================================================
# 詳細説明タブ
# ============================================================
HELP_TABS = [
    ("④ 議事録作成（使い方）", MINUTES_USAGE_TEXT),
    ("プロンプト構造", PROMPT_STRUCTURE_TEXT),
    ("モデル/料金", MODEL_COST_TEXT),
    ("GPTに送るプロンプト（原文）", PROMPT_TEXT),
]